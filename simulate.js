const { fireNotification } = require('./lib/fire-notification-sync.js');
// using a dummy script since lib/fire-notification uses ES modules and alias imports, which is hard to execute in pure node without setup.
